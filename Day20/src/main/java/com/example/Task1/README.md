TASK 1: Maven Assessment

Objective

Demonstrate understanding of Maven by creating, configuring, building, and packaging the Employee Management System project.

Activities Completed

* Created and configured a Maven project.
* Configured project dependencies in pom.xml.
* Resolved project dependencies using Maven.
* Cleaned the project using mvn clean.
* Compiled the project using mvn compile.
* Executed tests using mvn test.
* Packaged the application using mvn package.
* Generated the project JAR file inside the target directory.

Maven Commands Used

mvn clean
mvn compile
mvn test
mvn package

Dependencies

The project uses Maven dependencies for:

* PostgreSQL JDBC Driver
* SLF4J API
* Logback Classic

Build Result

The Maven project was successfully compiled and packaged.

The generated JAR file is available inside the:

target/

directory.

Acceptance Criteria

* Maven project builds successfully.
* Dependencies are resolved correctly.
* Project compiles successfully.
* Tests are executed successfully.
* JAR file is generated successfully.

Evidence

Screenshots of the Maven build and JAR generation are available in the screenshots folder.